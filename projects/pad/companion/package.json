{
  "name": "pad-companion",
  "version": "0.1.0",
  "private": true,
  "description": "Daemon companion del macropad HIOS: lee estado real de la PC (volumen, mic, temps CPU/GPU) y lo empuja al device via POST /api/state.",
  "scripts": {
    "build": "tsc && node scripts/copy-public.mjs",
    "start": "node dist/index.js",
    "dev": "npm run build && node dist/index.js"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0"
  }
}
